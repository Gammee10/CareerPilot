# Frontend build context (apps/frontend) — keep node_modules, build output,
# and local-only files out of the image. Next.js standalone output is built
# inside the image; a host-built .next must never leak in.
node_modules
.next
coverage
*.log
.env
.env.*
*.tsbuildinfo
next-env.d.ts
